<h2><%=l(:label_invoice_template)%></h2>

<%= labelled_form_for :invoice_template, @invoice_template, :url => { :action => 'update', :project_id => @project } do |f| %>
<%= render :partial => 'invoice_templates/form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>
